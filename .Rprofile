source("P:/R/common.Rprofile")
    
# setDefaults(q, save="no")
    # useDefaults(q)
    Sys.setenv(PDFLATEX="xelatex")
    setInternet2(TRUE) 
    Sys.setenv(R_GSCMD = '"C:/Program Files (x86)/gs/gs8.54/bin/gswin32c.exe"')
    Sys.setenv(SDMX_CONF = "P:/R/configuration.properties") # comment this out if P drive connection slow and not using SDMX
   
